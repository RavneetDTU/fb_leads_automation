# Previous Issues

Fast lookup, not narrative — grep this before assuming something is a new
problem. Full reasoning for each lives in `DECISIONS.md` if you need more
context than the row gives you.

| # | Category | Symptom | Root Cause | Fix | Prevention Rule | Regression Test? |
|---|---|---|---|---|---|---|
| 1 | Meta ingestion | Leads misattributed to the wrong ad/campaign | Per-lead attribution derived from the loop variable instead of the API response's own `ad_id` | Request `ad_id`/`ad_name` explicitly in the leads call, trust the response's own value | Never derive per-lead attribution from loop/iteration context | Yes |
| 2 | Meta ingestion | Phone parsing failed on custom form field names | Only standard field names were checked | Two-tier parser: exact match, then fuzzy on unmapped keys | Exact match always wins over fuzzy, even when they disagree | Yes — 3 real fixture lead IDs |
| 3 | Wati integration | Wati API 404s on some endpoints | V1 vs V3 endpoint versioning differs across tenants | Dual-endpoint client, try V1 then fall back to V3 | Never assume one endpoint version is universal | Pending (Phase 2) |
| 4 | Wati integration | Template send returned `200 OK` but `result: false` | Sent numeric positional params instead of the template's actual named schema | Fetch `customParams` schema before every send | Never assume positional/numeric param keys | Pending (Phase 2) |
| 5 | Deployment | Backend crash-looped (26,850+ retries) | An orphaned manually-started `uvicorn` process held the port outside systemd | Killed the orphaned process, restarted via systemd | Never start the service manually outside systemd | Manual (`DEPLOY.md`) |
| 6 | Safety | Historical leads were eligible for automated sends | `is_old_lead` treated as a soft/time-windowed flag instead of a structural guarantee | Structural, non-patchable `is_old_lead` column set once at insert | `is_old_lead` never exposed on any PATCH/PUT | Yes |
| 7 | Safety | Real historical leads got `is_old_lead: false` on what should've been the first sync | First-sync check keyed off "does any row exist in `meta_sync_runs`" — a failed zero-lead run still inserted a row | Changed check to "has any lead ever been successfully created" | First-sync detection must key off lead existence, not sync-run existence | Yes |
| 8 | Data integrity | All leads attached to a placeholder campaign (`"Campaign 1"` / `meta_campaign_id: "c1"`) | Hardcoded fallback campaign query (`db.query(Campaign).first()`) | Removed fallback; strict resolution via `ad_id → meta_form_mappings → campaign` | No default/fallback campaign assignment, ever — unresolvable attribution is a loud error, not a silent default | Yes |
| 9 | Testing | Test fixtures (e.g. `"Engaged User"`, `+27829990000`) contaminated the live/dev database | pytest wrote into `hal_v2` instead of an isolated schema | Isolated `hal_test` schema via `conftest.py` override | pytest must never touch `hal_v2` | Yes |
| 10 | Process | `hal_v2` got wiped and reseeded as a side effect of unrelated tickets, repeatedly | No rule against ad hoc truncation | Explicit ban stated and confirmed | `hal_v2` truncation only as the explicit, sole subject of a ticket | Process rule, not code |
| 11 | Frontend integration | Every page showed "Failed to fetch" | CORS preflight blocked — no allow-origin for the frontend's actual domain | Explicit origin allow-list in `CORSMiddleware` | Never wildcard, explicit origins only | Yes |
| 12 | Frontend integration | Campaign detail pages 404'd on click | `GET /api/campaigns/{id}/leads` was never implemented, despite being in the original contract | Added the route, matching the documented `{total, items}` shape | Diff the registered route table against the documented contract regularly — don't discover gaps one click at a time | Yes |
| 13 | Schema integrity | 4 tables (`messages`/`notes`/`whatsapp_template_cache`/`calendar_configs`) silently drifted from `models.py` | Initial Alembic migration was autogenerated against a legacy database; `models.py` was hand-written separately for `backendv2` and never reconciled against it | Reconciled `models.py` to match live DB exactly; consolidated into one clean, re-verified migration | All schema changes exclusively via `alembic upgrade head` — no manual `psycopg2` DDL scripts, ever | Yes |
| 14 | Schema integrity | An enum type was created unscoped in `public` instead of schema-qualified inside `hal_v2` | Migration authored inconsistently with the initial schema-qualified convention | DDL correction, verified via `pg_type`/`information_schema` before stamping | Verify actual DB state empirically before trusting a migration stamp reflects reality | Yes |
| 15 | Testing | A resubmission cross-campaign attribution test gave a false pass | The test submitted a duplicate under the *same* campaign the lead was already in, so the overwrite logic was never actually exercised | Redone against a genuinely different campaign, confirmed the attribution actually switched | A test must exercise the specific branch being claimed, not just execute without error | Yes |
| 16 | Frontend/backend integration | Frontend shipped and deployed a page (`WhatsAppInboxPage`) polling backend endpoints that don't exist | No cross-check between what the frontend calls and what the backend actually has registered before shipping | Confirmed via direct curl (`404`); inbox router now top of Phase 2 | Diff the frontend's called endpoints against the backend's actual route table before considering a page "done" — on both sides | Pending (Phase 2) |